# nata

A CLI tool that sorts files by extension or date.

## How to install

Make sure you have `rustc` and `cargo` in your system.

Clone this project, then install with this command:

```bash
cargo install --path .
```

Now you can use `nata` as a command line tool.

##
